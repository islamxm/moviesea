import { UIStatus } from "../types/ui";

export const uiStatus:Record<UIStatus, UIStatus> = {
  'default': 'default',
  'error': 'error',
  'loading': 'loading',
  'success': 'success',
  'warning': 'warning'
}